<!DOCTYPE html>
<html>
<head>
  <title>Student Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="p-4">
  <h1>Student Details</h1>

  <% if (student) { %>
    <div class="card" style="width: 18rem;">
      <img src="/static/<%= student.id %>.jpg" class="card-img-top" alt="Student Picture" />
      <div class="card-body">
        <h5 class="card-title"><%= student.name %></h5>
        <p class="card-text"><strong>ID:</strong> <%= student.id %></p>
        <p class="card-text"><strong>Department:</strong> <%= student.department %></p>
        <p class="card-text"><strong>CGPA:</strong> <%= student.cgpa %></p>
        <p class="card-text"><strong>Date of Birth:</strong> <%= student.dob %></p>
      </div>
    </div>
  <% } else { %>
    <p>Student not found!</p>
  <% } %>

  <a href="/student-list" class="btn btn-secondary mt-3">Back to Student List</a>
  <form action="/delete-student/<%= student.id %>" method="POST">
    <button type="submit" class="btn btn-danger mt-3">Delete Student</button">
  </form>
</body>
</html>
